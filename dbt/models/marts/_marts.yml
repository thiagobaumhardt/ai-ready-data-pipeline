version: 2

models:
  - name: fct_encounters
    description: One row per encounter, consumption-ready. Cardiology classification comes from int_encounters_classified.
    columns:
      - name: encounter_id
        tests:
          - unique
          - not_null
      - name: is_cardiology
        tests:
          - not_null
