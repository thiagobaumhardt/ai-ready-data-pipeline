version: 2

models:
  - name: fct_encounters
    description: One row per encounter, with is_cardiology derived from icd10_code (ICD-10 chapter IX, codes starting with "I").
    columns:
      - name: encounter_id
        tests:
          - unique
          - not_null
      - name: is_cardiology
        tests:
          - not_null
